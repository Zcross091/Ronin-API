name: Daily Manga Miner
on:
  schedule:
    # Runs at 17:10 UTC (10:40 PM IST) every day
    - cron: '10 17 * * *'
  workflow_dispatch: # Allows manual trigger from the GitHub Actions tab

jobs:
  mine-manga:
    runs-on: ubuntu-latest
    timeout-minutes: 60 # Increased timeout since fetching 300 manga takes time
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Install ts-node globally
        run: npm install -g ts-node typescript

      - name: Run Top 300 Manga Miner
        run: ts-node manga_miner.ts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
